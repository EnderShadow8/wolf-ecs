{
  "name": "wolf-ecs",
  "version": "1.1.1",
  "description": "An entity component system framework for JavaScript and TypeScript",
  "keywords": [
    "ecs",
    "entity component system",
    "gamedev"
  ],
  "main": "wolf-ecs.js",
  "types": "wolf-ecs.d.ts",
  "type": "module",
  "scripts": {
    "test": "c8 mocha",
    "pub": "npm run build && (cp package.json README.md dist; cd dist; npm publish && (cd ../; npm run gh-release))",
    "gh-release": "gh release create $(git describe --tags --abbrev=0) dist/*.js dist/*.d.ts -n \"\"",
    "min": "cd dist; uglifyjs wolf-ecs.js -o wolf-ecs.min.js -cm --no-annotations",
    "build": "rm -rf build dist; tsc; rollup -c; npm run min; npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EnderShadow8/wolf-ecs.git"
  },
  "author": "EnderShadow8",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/EnderShadow8/wolf-ecs/issues"
  },
  "homepage": "https://github.com/EnderShadow8/wolf-ecs",
  "devDependencies": {
    "chai": "^4.3.4",
    "rollup-plugin-dts": "^3.0.2",
    "typescript": "^4.3.2",
    "uglify-js": "^3.13.8"
  }
}
